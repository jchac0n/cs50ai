Before playing you'll need to install the required Python package (pygame):

In the directory for the project, run "pip3 install -r requirements.txt"
-----------------------------------------------------------------------------
Once installed, you can begin playing tic-tac-toe with the AI:

In the directory for the project, run "python runner.py"
